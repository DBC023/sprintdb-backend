spring:
  application:
    name: sprintdb
  datasource:
    # Agregamos 'jdbc:' al inicio de la URL maestra de Railway
    url: jdbc:${DATABASE_URL}
    username: ${PGUSER}
    password: ${PGPASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: ${PORT:8790}
  servlet:
    context-path: /sprintdb/api